#pragma once

// Force XP targeting if not already set
#ifndef _WIN32_WINNT
#define _WIN32_WINNT 0x0501
#endif

#if _WIN32_WINNT != 0x0501
#error Error: This PC spots a Wrong windows this pc doesnt work in any windows so im sparing you! Correct Windows can let you run it so cannot execute this program it only works in: Windows XP 32x and 64x
#endif


static void xp_pass_message() {
  
}
